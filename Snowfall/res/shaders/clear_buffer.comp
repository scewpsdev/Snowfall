#version 460


layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(set = 1, binding = 0) writeonly buffer Buffer {
	uint data[];
};


void main()
{
	uint idx = gl_GlobalInvocationID.x;
	data[idx] = 0;
}
